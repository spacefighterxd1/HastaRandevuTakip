@model IEnumerable<HastaRandevuTakip.Models.Doktor>

@{
    ViewData["Title"] = "Online Randevu Sistemi";
}

<div class="text-center mb-5">
    <h1 class="display-4">🏥 Online Randevu Sistemi</h1>
    <p class="lead">Randevu almak için doktorunuzu seçin</p>
    <a asp-action="Randevularim" class="btn btn-outline-primary btn-lg mt-3">
        📋 Randevularımı Görüntüle
    </a>
</div>

<div class="row">
    @if (Model != null && Model.Any())
    {
        @foreach (var doktor in Model)
        {
        <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
                <div class="card-body text-center">
                    <div class="mb-3">
                        @if (!string.IsNullOrEmpty(doktor.FotoUrl))
                        {
                            <img src="@doktor.FotoUrl" alt="@doktor.AdSoyad" 
                                 class="rounded-circle" style="width: 120px; height: 120px; object-fit: cover;">
                        }
                        else
                        {
                            <div class="rounded-circle bg-primary text-white d-inline-flex align-items-center justify-content-center" 
                                 style="width: 120px; height: 120px; font-size: 3rem;">
                                👨‍⚕️
                            </div>
                        }
                    </div>
                    <h4 class="card-title">@doktor.AdSoyad</h4>
                    <p class="text-primary fw-bold">@doktor.UzmanlikAlani</p>
                    <p class="card-text text-muted">@doktor.Aciklama</p>
                </div>
                <div class="card-footer bg-transparent border-0 text-center pb-3">
                    <a asp-action="RandevuFormu" asp-route-doktorId="@doktor.Id" 
                       class="btn btn-primary btn-lg w-75">
                        📅 Randevu Al
                    </a>
                    <a asp-action="DoktorDetay" asp-route-id="@doktor.Id" 
                       class="btn btn-outline-secondary btn-sm w-75 mt-2">
                        👁️ Detaylar
                    </a>
                </div>
            </div>
        </div>
        }
    }
</div>

@if (Model == null || !Model.Any())
{
    <div class="alert alert-warning text-center">
        <h5>Henüz doktor bulunmamaktadır.</h5>
    </div>
}

