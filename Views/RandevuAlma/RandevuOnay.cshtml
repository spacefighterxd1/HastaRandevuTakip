@model HastaRandevuTakip.Models.Randevu

@{
    ViewData["Title"] = "Randevu Onayı";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg border-success">
                <div class="card-header bg-success text-white text-center">
                    <h2 class="mb-0">✅ Randevunuz Başarıyla Oluşturuldu!</h2>
                </div>
                <div class="card-body">
                    <div class="text-center mb-4">
                        <div class="display-1 text-success">✓</div>
                        <h4 class="text-success">Randevu Numaranız: <strong>#@Model.Id</strong></h4>
                    </div>

                    <div class="alert alert-info">
                        <strong>Önemli:</strong> Randevu numaranızı not alınız. Randevu sorgulama için gerekli olacaktır.
                    </div>

                    <h5 class="mb-3">📋 Randevu Detayları</h5>
                    <dl class="row">
                        <dt class="col-sm-4">Hasta Adı:</dt>
                        <dd class="col-sm-8"><strong>@Model.Hasta?.AdSoyad</strong></dd>

                        <dt class="col-sm-4">Doktor:</dt>
                        <dd class="col-sm-8"><strong>@Model.Doktor?.AdSoyad</strong> - @Model.Doktor?.UzmanlikAlani</dd>

                        <dt class="col-sm-4">Randevu Tarihi:</dt>
                        <dd class="col-sm-8"><strong>@Model.RandevuTarihi.ToString("dd.MM.yyyy HH:mm")</strong></dd>

                        <dt class="col-sm-4">Poliklinik:</dt>
                        <dd class="col-sm-8">@Model.Poliklinik</dd>

                        <dt class="col-sm-4">Rahatsızlık:</dt>
                        <dd class="col-sm-8">@Model.Rahatsizlik</dd>

                        <dt class="col-sm-4">Durum:</dt>
                        <dd class="col-sm-8">
                            @switch (Model.Durum)
                            {
                                case RandevuDurumu.Bekliyor:
                                    <span class="badge bg-warning">Bekliyor</span>
                                    break;
                                case RandevuDurumu.Onaylandi:
                                    <span class="badge bg-success">Onaylandı</span>
                                    break;
                                case RandevuDurumu.IptalEdildi:
                                    <span class="badge bg-danger">İptal Edildi</span>
                                    break;
                                case RandevuDurumu.Tamamlandi:
                                    <span class="badge bg-info">Tamamlandı</span>
                                    break;
                            }
                        </dd>

                        @if (!string.IsNullOrEmpty(Model.Notlar))
                        {
                            <dt class="col-sm-4">Notlar:</dt>
                            <dd class="col-sm-8">@Model.Notlar</dd>
                        }
                    </dl>

                    <div class="alert alert-warning mt-4">
                        <strong>Bilgilendirme:</strong> Randevunuz onaylandığında size telefon veya e-posta ile bilgi verilecektir.
                    </div>

                    <div class="text-center mt-4">
                        <a asp-action="Index" class="btn btn-primary btn-lg">
                            🏠 Ana Sayfaya Dön
                        </a>
                        <a asp-action="Randevularim" class="btn btn-outline-primary btn-lg ms-2">
                            📋 Randevularımı Görüntüle
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

