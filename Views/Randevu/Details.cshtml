@model HastaRandevuTakip.Models.Randevu

@{
    ViewData["Title"] = "Randevu Detayları";
}

<h2>📋 Randevu Detayları</h2>
<hr />

<div class="row">
    <div class="col-md-8">
        <dl class="row">
            <dt class="col-sm-4">Hasta:</dt>
            <dd class="col-sm-8">
                <strong>@Model.Hasta?.AdSoyad</strong><br>
                <small class="text-muted">TC: @Model.Hasta?.TCKimlikNo | Tel: @Model.Hasta?.Telefon</small>
            </dd>

            <dt class="col-sm-4">Randevu Tarihi:</dt>
            <dd class="col-sm-8">
                <strong>@Model.RandevuTarihi.ToString("dd.MM.yyyy HH:mm")</strong>
            </dd>

            <dt class="col-sm-4">Doktor:</dt>
            <dd class="col-sm-8">
                <strong>@(Model.Doktor?.AdSoyad ?? Model.DoktorAdi ?? "Belirtilmemiş")</strong>
                @if (Model.Doktor != null)
                {
                    <br><small class="text-muted">@Model.Doktor.UzmanlikAlani</small>
                }
            </dd>

            <dt class="col-sm-4">Poliklinik:</dt>
            <dd class="col-sm-8">@(Model.Poliklinik ?? "Belirtilmemiş")</dd>

            <dt class="col-sm-4">Durum:</dt>
            <dd class="col-sm-8">
                @switch (Model.Durum)
                {
                    case RandevuDurumu.Bekliyor:
                        <span class="badge bg-warning">Bekliyor</span>
                        break;
                    case RandevuDurumu.Onaylandi:
                        <span class="badge bg-success">Onaylandı</span>
                        break;
                    case RandevuDurumu.IptalEdildi:
                        <span class="badge bg-danger">İptal Edildi</span>
                        break;
                    case RandevuDurumu.Tamamlandi:
                        <span class="badge bg-info">Tamamlandı</span>
                        break;
                }
            </dd>

            <dt class="col-sm-4">Rahatsızlık:</dt>
            <dd class="col-sm-8">@(Model.Rahatsizlik ?? "Belirtilmemiş")</dd>

            <dt class="col-sm-4">Notlar:</dt>
            <dd class="col-sm-8">@(Model.Notlar ?? "Not bulunmamaktadır.")</dd>

            <dt class="col-sm-4">Oluşturulma Tarihi:</dt>
            <dd class="col-sm-8">@Model.CreatedDate.ToString("dd.MM.yyyy HH:mm")</dd>
        </dl>

        <div class="mt-4">
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">
                ✏️ Düzenle
            </a>
            <a asp-action="Index" class="btn btn-secondary">Geri Dön</a>
        </div>
    </div>
</div>


