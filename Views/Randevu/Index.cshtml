@model IEnumerable<HastaRandevuTakip.Models.Randevu>

@{
    ViewData["Title"] = "Randevular";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>📅 Randevu Listesi</h2>
    <a asp-action="Create" class="btn btn-primary">
        <span>➕</span> Yeni Randevu Ekle
    </a>
</div>

<div class="card">
    <div class="card-body">
        <form asp-action="Index" method="get" class="mb-3">
            <div class="row">
                <div class="col-md-8">
                    <input type="text" name="searchString" class="form-control" 
                           placeholder="Hasta adı, doktor, poliklinik ara..." 
                           value="@ViewBag.CurrentFilter" />
                </div>
                <div class="col-md-4">
                    <button type="submit" class="btn btn-outline-primary">🔍 Ara</button>
                    <a asp-action="Index" class="btn btn-outline-secondary">Temizle</a>
                </div>
            </div>
        </form>

        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>
                            <a asp-action="Index" asp-route-sortOrder="@ViewBag.HastaSortParm" 
                               class="text-white text-decoration-none">
                                Hasta @if (ViewBag.CurrentSort == "Hasta") { <span>↑</span> }
                                @if (ViewBag.CurrentSort == "Hasta_desc") { <span>↓</span> }
                            </a>
                        </th>
                        <th>
                            <a asp-action="Index" asp-route-sortOrder="@ViewBag.TarihSortParm" 
                               class="text-white text-decoration-none">
                                Randevu Tarihi @if (ViewBag.CurrentSort == "Tarih") { <span>↑</span> }
                                @if (ViewBag.CurrentSort == "Tarih_desc") { <span>↓</span> }
                            </a>
                        </th>
                        <th>Doktor</th>
                        <th>Poliklinik</th>
                        <th>Rahatsızlık</th>
                        <th>Durum</th>
                        <th class="text-end">İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <strong>@item.Hasta?.AdSoyad</strong><br>
                                <small class="text-muted">@item.Hasta?.Telefon</small>
                            </td>
                            <td>
                                <strong>@item.RandevuTarihi.ToString("dd.MM.yyyy HH:mm")</strong>
                            </td>
                            <td>
                                <strong>@item.Doktor?.AdSoyad</strong><br>
                                <small class="text-muted">@item.Doktor?.UzmanlikAlani</small>
                            </td>
                            <td>@item.Poliklinik</td>
                            <td>
                                <small>@(item.Rahatsizlik?.Length > 50 ? item.Rahatsizlik.Substring(0, 50) + "..." : item.Rahatsizlik)</small>
                            </td>
                            <td>
                                @switch (item.Durum)
                                {
                                    case RandevuDurumu.Bekliyor:
                                        <span class="badge bg-warning">Bekliyor</span>
                                        break;
                                    case RandevuDurumu.Onaylandi:
                                        <span class="badge bg-success">Onaylandı</span>
                                        break;
                                    case RandevuDurumu.IptalEdildi:
                                        <span class="badge bg-danger">İptal Edildi</span>
                                        break;
                                    case RandevuDurumu.Tamamlandi:
                                        <span class="badge bg-info">Tamamlandı</span>
                                        break;
                                }
                            </td>
                            <td class="text-end">
                                <div class="btn-group" role="group">
                                    <a asp-action="Details" asp-route-id="@item.Id" 
                                       class="btn btn-sm btn-info">👁️ Detay</a>
                                    <a asp-action="Edit" asp-route-id="@item.Id" 
                                       class="btn btn-sm btn-warning">✏️ Düzenle</a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" 
                                       class="btn btn-sm btn-danger">🗑️ Sil</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @if (!Model.Any())
        {
            <div class="alert alert-info text-center">
                <p class="mb-0">Henüz randevu kaydı bulunmamaktadır.</p>
                <a asp-action="Create" class="btn btn-primary mt-2">İlk Randevuyu Oluştur</a>
            </div>
        }
    </div>
</div>


